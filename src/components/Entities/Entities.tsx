import { FC } from 'react';
import { Link } from 'react-router-dom';
import styled from 'styled-components';


const Wrapper = styled.div``;

export const Entities: FC = () => {

    return (
        <Wrapper>
            Jakiś tekst Entities.
        </Wrapper>
    );
};